<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guoxin.demo.mapper.UserMapper">


    <insert id="insert" parameterType="com.guoxin.demo.model.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user(id,username,password)
        VALUES(#{id,jdbcType=INTEGER},#{username,jdbcType=VARCHAR},#{password,jdbcType=VARCHAR})
    </insert>
    <delete id="delete" parameterType="int">
        DELETE FROM user WHERE id=#{id,jdbcType=INTEGER}
    </delete>
    <!--<insert id="batchInsert" parameterType="list">-->
        <!--INSERT INTO user(id,username,password)-->
        <!--VALUES-->
        <!--<foreach collection="list" item="item" index="index" separator=",">-->
            <!--(null,#{item.username},#{item.password})-->
        <!--</foreach>-->
        <!--ON DUPLICATE KEY UPDATE-->
        <!--id=VALUES(id),username=VALUES(username),password=VALUES(password)-->
    <!--</insert>-->
    <!--<update id="update">-->
        <!--UPDATE user-->
        <!--<trim prefix="SET" suffixOverrides=",">-->
			<!--<if test="username!=null">-->
                <!--username=#{username},-->
            <!--</if>-->
			<!--<if test="password!=null">-->
                <!--password=#{password},-->
            <!--</if>-->
        <!--</trim>-->
        <!--WHERE id=#{id}-->
    <!--</update>-->

    <sql id="SELECT_FIELD">
        id,username,password
    </sql>
</mapper>